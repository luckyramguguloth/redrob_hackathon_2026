# Redrob Hackathon — Submission Metadata
#
# Filled per challenge rules and portal submission requirement.

# ============================================================================
# Team identity
# ============================================================================
team_name: "Laxman_HYD"

primary_contact:
  name: "Laxman"
  email: "luckyramguguloth@gmail.com"
  phone: "+91-9949447607"

team_members:
  - name: "Laxman"
    email: "luckyramguguloth@gmail.com"
    role: "ML Engineer"

# ============================================================================
# Code and reproducibility
# ============================================================================
github_repo: "https://github.com/laxman/redrob-ranker"

reproduce_command: "python rank.py --candidates ./candidates.jsonl --out ./result_candidates.csv"

# ============================================================================
# Compute environment
# ============================================================================
compute:
  platform: "Local PC (Windows)"
  cpu_cores: 8
  ram_gb: 16
  python_version: "3.10.0"
  os: "Windows 11"
  uses_gpu_for_inference: false
  has_network_during_ranking: false
  pre_computation_required: false
  pre_computation_time_minutes: 0

# ============================================================================
# AI tools declaration
# ============================================================================
ai_tools_used:
  - "Claude"
  - "Gemini"
  - "ChatGPT"

ai_usage_summary: |
  Used AI tools (Claude, Gemini, and ChatGPT) as programming assistants to identify performance bottlenecks in the parsing module and to refactor tiered loops into O(1) dictionary lookups. Additionally, utilized AI tools to design and debug CSS styling for the Streamlit dashboard layout. No candidate profile data was shared with any external AI models.


# ============================================================================
# Approach summary
# ============================================================================
methodology_summary: |
  A robust, rule-based candidate ranker tailored for the Senior AI Engineer role. It applies a multi-stage pipeline: (1) schema validation and parsing, (2) rule-based honeypot detection to identify and flag inconsistent profiles, (3) knockout filtering, (4) tiered skill scoring against required search/retrieval profiles, (5) career trajectory analysis, and (6) a multiplicative behavioral modifier based on platform engagement signals. This ensures high-intent, technically strong candidates are ranked highest.

# ============================================================================
# Declarations
# ============================================================================
declarations:
  read_submission_spec: true
  code_is_original_work: true
  no_collusion: true
  honeypot_check_done: true
  reproduction_tested: true
